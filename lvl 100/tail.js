// tail.js
const positions = [];

function updateTail(top, left) {
  const canvas = document.querySelector('.canvas');

  // 💡 TODO: Store new position in the array
  // 💡 TODO: Remove oldest position if the array is too long
  // 💡 TODO: Remove old tail elements and create new ones based on positions
}
